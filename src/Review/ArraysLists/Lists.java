/*package Review.ArraysLists;

import java.util.ArrayList;
import java.util.List;

public class Lists {*/
    /* public static void main(String[] args) {
        printList();
        printArrays();

        List<Integer> listNumber = new ArrayList<>();
        listNumber.add(1);
        listNumber.add(2);
        listNumber.add(3);

        int min = getListss(listNumber);
        System.out.println(min);

    }

    public static void printList() {
        List<String> createList = new ArrayList<>();
        createList.add("a");
        createList.add("b");
        createList.add("c");

        for (int i = 0; i < createList.size(); i++) {
            System.out.print(createList.get(i) + " ");
        }
        System.out.println("");
        for (String i : createList) {
            System.out.print(i + " ");
        }
        System.out.println("");

        createList.remove(1);
        createList.set(0, "k");
        System.out.println(createList);
    }
    public static void printArrays() {
        String[] createArrays = new String[3];
        createArrays[0] = "x";
        createArrays[1] = "y";
        createArrays[2] = "z";

        for (int i = 0; i < createArrays.length; i++) {
            System.out.print(createArrays[i] + " ");
        }
        for (String i : createArrays) {
            System.out.print(i + " ");
        }
    }
    public static int getListss(ArrayList<Integer> getList) {
        int min = getList.get(0);
        for (int i : getList) {
            if (i < min) {
                min = i;
            }
        }
        return min;
    }
}
*/